# DEPRECATED — replaced by chicago_crime_ml_daily_dag.py
#
# This file is intentionally left as a stub so Airflow does not raise an
# ImportError when it scans the dags directory during the transition.
# The DAG definition has been removed; Airflow will find no DAG in this
# file and the old run history will remain accessible in the UI.
#
# Feature engineering now runs as the first task of chicago_crime_ml_daily_dag.
